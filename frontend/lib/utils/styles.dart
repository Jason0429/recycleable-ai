import 'package:flutter/material.dart';

class StyleConstants {
  static const titleFontSize = 30.0;
  static const screenPadding =
      EdgeInsets.symmetric(horizontal: 20, vertical: 40);
  static const fieldPadding = EdgeInsets.symmetric(horizontal: 20, vertical: 0);
  static final borderRadius = BorderRadius.circular(30.0);
}
